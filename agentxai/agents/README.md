# agents

Contains all four LangChain agent implementations: the Orchestrator (entry point, planner, router), Specialist A (symptom analysis), Specialist B (evidence retrieval), and the Synthesizer (reads specialist memory, produces final diagnosis with no tool calls).
