# store

SQLite-backed persistence layer for all XAI schemas — one table per schema (trajectory_events, agent_plans, tool_use_events, memory_diffs, agent_messages, causal_edges, accountability_reports) with read/write helpers used by the XAI runtime layer.
